name: wiinz_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# The +N suffix is the Android versionCode. Play rejects any upload whose
# versionCode is not strictly higher than one already uploaded, and +1 was
# consumed by the first AAB, so this MUST be bumped for every Play upload.
# iOS ignores both values: codemagic.yaml passes --build-name/--build-number
# explicitly, so the iOS build number comes from Codemagic's own counter.
version: 1.0.1+4

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  qr_code_scanner_plus: ^2.0.10
  qr_flutter: ^4.1.0
  geolocator: ^14.0.3
  shared_preferences: ^2.5.5
  http: ^1.6.0
  go_router: ^17.3.0
  # google_fonts REMOVED deliberately. It fetched Cairo/Noto from
  # fonts.gstatic.com at first launch, so the app rendered Arabic in the
  # platform fallback font until that download finished — and permanently when
  # offline. Cairo is now bundled from assets/fonts. See lib/theme/app_theme.dart.
  material_symbols_icons: ^4.2951.0
  maplibre_gl: ^0.26.2
  url_launcher: ^6.3.2
  permission_handler: ^12.0.3
  font_awesome_flutter: ^11.0.0
  share_plus: ^13.2.0
  flutter_local_notifications: ^18.0.1
  image_picker: ^1.1.2
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# App launcher icon generation. Run: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/WIINZ_icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#1F7A1F"
  adaptive_icon_foreground: "assets/icon/WIINZ_icon.png"
  # The App Store rejects a 1024x1024 icon that carries an alpha channel, and
  # iOS icons must not be transparent anyway — flatten onto the brand green.
  remove_alpha_ios: true
  background_color_ios: "#1F7A1F"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/ooredoo_ad.jpg
    - assets/images/splash-screen.jpg
    - assets/images/wiin-logo-green.png
    - assets/images/wiin-logo-white.png

  # Cairo is the app's only type family — it covers Arabic, Latin and the
  # French accents, so nothing falls back to a system font. Bundled rather
  # than fetched at runtime; see the google_fonts note in dependencies.
  # Weights match what the UI actually asks for: w400 body, w600/w700/w800
  # headings, w900 display.
  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
          weight: 400
        - asset: assets/fonts/Cairo-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Cairo-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Cairo-Black.ttf
          weight: 900

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
